# Deploy Fly.io review apps for authorized users
name: Fly Review App Deploy

on:
  pull_request:
    types: [opened, reopened, synchronize, closed]
  workflow_dispatch: # Allow manual trigger on PRs

# Minimal permissions for security
permissions:
  contents: read
  pull-requests: write
  checks: write
  statuses: write

env:
  FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
  # Allowlist of GitHub usernames allowed to deploy review apps
  DEPLOY_ALLOWLIST: "bstee615"
  # Set these to your Fly.io organization and preferred region
  FLY_REGION: ord
  FLY_ORG: personal

jobs:
  check-authorization:
    name: Check if user is authorized
    runs-on: ubuntu-latest
    outputs:
      authorized: ${{ steps.check.outputs.authorized }}
    steps:
      - name: Check authorization
        id: check
        run: |
          # For manual triggers, check the actor
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TRIGGER_USER="${{ github.actor }}"
          # For Copilot PRs, check who triggered the workflow (the human user, not the bot)
          elif [ "${{ github.event.pull_request.user.login }}" = "copilot-swe-agent[bot]" ]; then
            # For Copilot PRs, we need to check the sender (who created/updated the PR)
            # or fall back to checking if the bot itself is allowed
            TRIGGER_USER="${{ github.event.sender.login }}"
            # If sender is also the bot (automated events), allow it since it was triggered by an authorized user
            if [ "$TRIGGER_USER" = "copilot-swe-agent[bot]" ] || [ "$TRIGGER_USER" = "github-actions[bot]" ]; then
              echo "✅ Copilot bot action authorized (automated event)"
              echo "authorized=true" >> $GITHUB_OUTPUT
              exit 0
            fi
          # For regular PRs, check the PR author
          else
            TRIGGER_USER="${{ github.event.pull_request.user.login }}"
          fi
          
          echo "Checking authorization for user: $TRIGGER_USER"
          
          # Check if user is in allowlist
          if echo "${{ env.DEPLOY_ALLOWLIST }}" | tr ',' '\n' | grep -qx "$TRIGGER_USER"; then
            echo "✅ User $TRIGGER_USER is authorized"
            echo "authorized=true" >> $GITHUB_OUTPUT
          else
            echo "❌ User $TRIGGER_USER is not authorized"
            echo "authorized=false" >> $GITHUB_OUTPUT
          fi

  review_app:
    name: Deploy review app
    runs-on: ubuntu-latest
    needs: check-authorization
    if: needs.check-authorization.outputs.authorized == 'true'
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    # Only run one deployment at a time per PR
    concurrency:
      group: pr-${{ github.event.number }}
    
    # Deploying apps with this "review" environment allows the URL for the app to be displayed in the PR UI
    environment:
      name: review
      url: ${{ steps.deploy.outputs.url }}
    
    steps:
      - name: Get code
        uses: actions/checkout@v4
      
      - name: Deploy PR app to Fly.io
        id: deploy
        uses: superfly/fly-pr-review-apps@1.2.1
